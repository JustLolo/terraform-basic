#!/usr/bin/env python3
# import subprocess
# import shlex


# someCommand = "this is just some random command that will return an error ASAP y press enter"
# print ("read -p 'Hit enter to run: {}".format(someCommand))



# print ('read -p \'Hit enter to run: "{}"\''.format(someCommand))

# subprocess.run('read -p \'Hit enter to run: "{}"\''.format(someCommand), shell=True)

# subprocess.run(shlex.split('read -p \'Hit enter to run: "{}"\''.format(someCommand)))
# subprocess.run("$c")
